# Iniciar Frontend

1. Instalar dependencias (primera vez):
```bash
cd frontend
npm install
```

2. Iniciar el frontend:
```bash
npm start
```

3. Abrir navegador en:
```
http://localhost:3000
```

Si ves una página en blanco, revisa la consola del navegador (F12) para ver el error.

